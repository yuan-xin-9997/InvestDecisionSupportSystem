# 需求规格说明书

**项目名称**：投资决策支持系统（InvestDecisionSupportSystem）
**版本**：v1.0（2026-08-30 首次发布）
**关联文档**：设计说明书.md、README.md

## 1. 项目背景与目标

用户个人投资标的覆盖股票（指数、ETF、个股）、黄金（期货、上海黄金交易所现货）与基金，行情数据已通过 vnpy 框架采集至 PostgreSQL 数据库。本系统旨在为个人投资决策提供统一的数据参考与记录工具：

1. 将已有行情数据以 K 线图/表格形式直观展示；
2. 记录投资日志（图文），沉淀每笔操作背后的思考；
3. 提供灵活的自定义数据存储（宏观、微观及其他跟踪指标）。

## 2. 用户角色

| 角色 | 权限 |
| ---- | ---- |
| 管理员（admin） | 全部页面与功能，含权限管理、系统配置 |
| 普通用户（user） | 由管理员分配可见页面（系统概览/行情展示/投资日志/数据管理/任务中心） |

用户名单与密码维护在 `src/data/password.txt`（格式 `username:password:role`），新用户首次登录自动同步进数据库；权限（角色、可见页面）在权限管理页维护。

## 3. 功能需求

### FR-1 登录认证

- FR-1.1 用户以用户名+密码登录，校验依据为 `password.txt`；密码错误返回明确提示。
- FR-1.2 登录成功颁发会话 token（有效期可在配置中调整，默认 7 天），前端保存并在后续请求头携带。
- FR-1.3 支持退出登录，退出后 token 立即失效。
- FR-1.4 `password.txt` 中新增的用户，首次成功登录时自动同步进数据库。
- FR-1.5 未登录访问任何业务接口返回 401。

### FR-2 权限管理（管理员）

- FR-2.1 展示可登录用户列表（含数据库已同步用户与 password.txt 中待同步用户）。
- FR-2.2 管理员可修改用户角色（admin/user）与可见页面集合。
- FR-2.3 admin 角色恒拥有全部页面，勾选禁用。
- FR-2.4 系统至少保留一名管理员，禁止将最后一个 admin 降级。
- FR-2.5 支持手动触发"从 password.txt 同步"。

### FR-3 行情展示

- FR-3.1 连接 PostgreSQL 行情库（vnpy 格式），读取 `dbbaroverview`/`dbtickoverview` 展示数据概览（品种、交易所、周期、记录数、起止时间）。
- FR-3.2 按 品种+交易所+周期（d/1h/1m）+ 日期范围 + 根数上限 查询 `dbbardata` 返回 K 线序列（时间正序）。
- FR-3.3 前端以 ECharts 蜡烛图展示，附 MA5/MA10/MA20 均线与成交量副图，支持缩放平移；可切换为表格视图。
- FR-3.4 行情库不可达时返回可读错误（503），页面展示错误提示而非白屏。
- FR-3.5 单次查询根数上限可配置（默认上限 5000），防止大查询拖垮数据库。

### FR-4 投资日志

- FR-4.1 支持新建日志：正文（文本）+ 多张图片 + 归属日期（默认当天，可指定）。
- FR-4.2 图片支持 jpg/png/gif/webp/bmp；单图大小限制可配置（默认 10MB），超限拒绝；超过长边阈值（默认 2000px）自动等比压缩。
- FR-4.3 图片按 年/月/日 目录存储于 `src/data/journal/`，数据库记录相对路径。
- FR-4.4 日志按时间线倒序展示（先按归属日期，再按创建时间）；支持日期范围与关键词筛选、分页。
- FR-4.5 支持编辑正文/日期、追加图片、删除单张图片、删除整篇日志（联动删除图片文件）。

### FR-5 数据管理（自定义跟踪数据）

- FR-5.1 支持创建/编辑/删除数据集，属性含名称（唯一）、分类（宏观/微观/其他）、单位、说明。
- FR-5.2 数据记录为"日期-数值-备注"，同一数据集同一天仅保留一条（重复保存覆盖更新）。
- FR-5.3 记录支持分页查询、日期范围筛选、单条编辑与删除。
- FR-5.4 折线图展示记录走势。
- FR-5.5 支持 CSV 批量导入（表头 `date,value,note`，UTF-8 或 GBK 编码，同日期覆盖，坏行跳过并统计）与 CSV 导出。

### FR-6 任务中心

- FR-6.1 展示任务列表：任务ID、名称、说明、最近一次运行状态与日志。
- FR-6.2 内置任务：
  - `postgres_check` 行情数据库连通性检查（探活 + 报告 K 线总记录数）；
  - `log_cleanup` 历史日志清理（删除超过保留期的按天切割日志）；
  - `journal_orphan_scan` 日志附件一致性检查（磁盘文件与数据库记录比对）。
- FR-6.3 支持立即运行任务（同步执行，记录开始/结束时间、状态、消息、耗时）。
- FR-6.4 支持查看每个任务的运行历史。

### FR-7 系统配置（管理员）

- FR-7.1 展示运行信息：系统名称、版本号、启动时间、运行时长、Python 版本、平台、服务器时间（北京时间）。
- FR-7.2 展示脱敏后的主配置（密码字段以 `******` 显示）与关键文件路径。

### FR-8 通用要求

- FR-8.1 版本号 = GitHub 提交数量（`git rev-list --count HEAD`），在侧边栏左下角展示。
- FR-8.2 系统展示的所有时间统一为北京时间（UTC+8）；naive 时间视为北京时间，带时区时间转换为北京时间。
- FR-8.3 浏览器 TAB 页显示系统专属图标（favicon）。
- FR-8.4 系统所有环境相关信息（IP、端口、用户名、密码、路径）一律配置化，禁止硬编码。

## 4. 非功能需求

| 类别 | 要求 |
| ---- | ---- |
| 技术栈 | 后端 FastAPI + SQLAlchemy；前端 Vue 3 + Element Plus + ECharts；本地库 SQLite；行情库 PostgreSQL（只读） |
| 安全 | 密码比对使用常量时间比较；token 随机 256bit；接口分级鉴权（登录→页面权限→管理员）；配置展示脱敏 |
| 性能 | 行情单查询限流（≤5000 根）；PostgreSQL 连接池复用；日志图片压缩存储 |
| 可维护性 | 按天切割日志并自动清理；API 文档自动生成（/api/docs） |
| 可测试性 | 42 个单元/接口/冒烟测试，测试环境隔离（临时目录 IDSS_HOME） |
| 部署 | 一键启停脚本（Linux/Windows）；Jenkins 30 分钟轮询自动部署；支持 systemd；首次部署自动初始化 data 目录与 password.txt |

## 5. 边界与约束

1. 本系统对 PostgreSQL 行情库**只读**，不写入任何行情数据。
2. 行情数据本身为北京时间，系统不做时区换算，直接展示。
3. 用户密码明文保存于 `password.txt`（由用户自行维护该文件的安全性），系统不提供页面修改密码。
4. 当前版本任务中心仅支持手动触发内置任务；定时调度作为后续增强项。
