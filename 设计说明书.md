# 设计说明书

**项目名称**：投资决策支持系统（InvestDecisionSupportSystem）
**版本**：v1.0（2026-08-30）
**关联文档**：需求规格说明书.md、README.md

## 1. 总体架构

```
浏览器 (Vue 3 + Element Plus + ECharts, Hash 路由)
        │  HTTP (/api/*, 静态资源)
        ▼
FastAPI 后端 (src/app/backend)
   ├── routers   API 路由层（auth/users/system/tasks/market/journal/datasets）
   ├── core      配置加载 / 日志切割 / 安全 / 认证依赖 / 北京时间工具
   ├── db        sqlite.py (本地 SQLAlchemy) + postgres.py (行情库 psycopg2 连接池)
   └── models    ORM 模型（users/tokens/journals/journal_images/datasets/dataset_records/tasks/task_runs）
        │                    │
        ▼                    ▼
   SQLite (src/data/app.sqlite3)   PostgreSQL 行情库 (vnpy: dbbardata 等, 只读)
```

- 生产模式下 FastAPI 直接托管前端构建产物 `src/app/frontend/dist`（SPA history 回退），部署机无需 Node。
- 前端开发模式使用 Vite（5173 端口）并将 `/api` 代理至后端（8620 端口）。

## 2. 目录与模块

```
src/
├── app/backend/
│   ├── main.py               # 应用工厂 create_app：初始化日志/数据库/路由/SPA 挂载；__main__ 启动 uvicorn
│   ├── core/
│   │   ├── config.py         # AppConfig：加载 config/app.json，默认值合并、路径推导、脱敏；IDSS_HOME 可重定向根目录（测试用）
│   │   ├── logger.py         # TimedRotatingFileHandler，按天切割，namer 定制为 app.YYYY-MM-DD.log
│   │   ├── security.py       # password.txt 解析、常量时间密码比对、token 生成
│   │   ├── timeutil.py       # 北京时间（UTC+8）工具：naive 视为北京时间，aware 转 CST
│   │   └── deps.py           # get_current_user / require_page(key) / require_admin 三级鉴权依赖
│   ├── db/
│   │   ├── sqlite.py         # 全局引擎（WAL 模式）、会话工厂、init_db（建表+内置任务）
│   │   └── postgres.py       # psycopg2 SimpleConnectionPool；overview/kline 查询；MarketDBError
│   ├── models.py             # 全部 ORM 模型，时间字段统一字符串（北京时间 "YYYY-MM-DD HH:MM:SS"）
│   └── routers/              # 见接口设计
├── app/frontend/             # Vue 3 工程（src/api.js 封装 fetch+token；views/ 七个页面）
├── config/app.json           # 唯一环境配置来源
├── data/                     # SQLite、password.txt、journal/年/月/日 图片
├── JenkinsConfig/            # Jenkinsfile、invest-dss.service
├── tests/                    # pytest（conftest 构建 IDSS_HOME 临时目录隔离环境）
└── logs/                     # app.log + app.日期.log + server.pid
```

## 3. 数据库设计

### 3.1 本地 SQLite

| 表 | 关键字段 | 说明 |
| --- | --- | --- |
| users | username(唯一)、role、pages(JSON)、last_login_at | 登录用户与页面权限；与 password.txt 同步 |
| tokens | token(PK)、username、expires_at | 会话；登录时清理过期/旧 token（单会话） |
| journals | content、trade_date(索引)、created_by | 投资日志正文 |
| journal_images | journal_id(索引)、original_name、rel_path、size | 图片附件，rel_path 相对工程根目录 |
| datasets | name(唯一)、category(宏观/微观/其他)、unit、description | 跟踪数据集 |
| dataset_records | dataset_id+date(唯一)、value、note | 指标记录，同日覆盖 |
| tasks | task_id(PK)、name、description、enabled | 任务定义（内置三条） |
| task_runs | task_id+started_at(索引)、status、message | 运行历史 |

### 3.2 行情 PostgreSQL（只读）

| 表 | 用途 |
| --- | --- |
| dbbaroverview | K 线数据概览（品种/周期/条数/起止时间） |
| dbbardata | K 线数据（symbol, exchange, interval, datetime, OHLCV, turnover, open_interest），datetime 为北京时间 |
| dbtickoverview / dbtickdata | Tick 概览与明细（概览用于展示，明细当前未消费） |

kline 查询：`WHERE symbol=? AND exchange=? AND "interval"=? [AND datetime 范围] ORDER BY datetime DESC LIMIT n`，取回后反转为时间正序；n 上限取配置 `postgres.query_limit_max`。

## 4. 接口设计（前缀 /api，认证头 Authorization: Bearer <token>）

| 方法 | 路径 | 权限 | 说明 |
| --- | --- | --- | --- |
| POST | /auth/login | 公开 | 登录，返回 token+user；同步用户、清理旧 token |
| POST | /auth/logout | 登录 | 注销 token |
| GET | /auth/me | 登录 | 当前用户信息 |
| GET | /users | admin | 用户列表（含未同步用户标注） |
| GET | /users/pages | admin | 可分配页面清单 |
| PUT | /users/{username} | admin | 修改角色/页面（保护最后一个 admin） |
| POST | /users/sync | admin | 从 password.txt 同步 |
| GET | /system/info | 登录 | 版本（git 提交数）、启动时间、Python 等 |
| GET | /system/config | admin | 脱敏配置 + 路径 |
| GET | /health | 公开 | 健康检查 |
| GET | /tasks | tasks 页面权限 | 任务列表+最近运行 |
| POST | /tasks/{id}/run | tasks 页面权限 | 立即运行 |
| GET | /tasks/{id}/runs | tasks 页面权限 | 运行历史 |
| GET | /market/overview | market 页面权限 | 行情概览（503 当库不可达） |
| GET | /market/kline | market 页面权限 | K 线查询（400 参数错误 / 503 库不可达） |
| POST | /journal | 登录 | 新建日志（multipart：content/trade_date/files[]） |
| GET | /journal | journal 页面权限 | 时间线查询（start/end_date、keyword、分页） |
| GET/PUT/DELETE | /journal/{id} | journal 页面权限 | 详情/编辑/删除 |
| POST | /journal/{id}/images | journal 页面权限 | 追加图片 |
| DELETE | /journal/{id}/images/{image_id} | journal 页面权限 | 删除单图 |
| GET | /journal/images/{image_id} | journal 页面权限 | 图片文件流 |
| GET/POST | /datasets | datasets 页面权限 | 列表（含记录数/最新值）/新建 |
| PUT/DELETE | /datasets/{id} | datasets 页面权限 | 编辑/删除（级联记录） |
| GET/POST | /datasets/{id}/records | datasets 页面权限 | 记录分页/同日覆盖写入 |
| DELETE | /datasets/records/{id} | datasets 页面权限 | 删除记录 |
| POST | /datasets/{id}/import | datasets 页面权限 | CSV 导入（UTF-8/GBK，同日覆盖，坏行跳过） |
| GET | /datasets/{id}/export | datasets 页面权限 | CSV 导出（RFC 5987 中文文件名） |

## 5. 关键设计决策

1. **配置单一来源**：`config/app.json` 为唯一环境信息来源；`AppConfig` 提供默认值合并、相对路径推导（data/logs/journal 均相对工程根）与脱敏输出；`IDSS_HOME` 环境变量重定向根目录供测试隔离。
2. **时间处理**：所有存储时间为北京时间字符串；`timeutil.to_cst` 统一 naive（视为北京时间）与 aware（转换）两种输入，满足"非北京时间需转换为北京时间展示"的要求。
3. **鉴权三级模型**：登录（401）→ 页面权限 require_page（403）→ 管理员 require_admin；前端路由守卫与菜单过滤同步生效，前后端双重校验。
4. **行情库故障隔离**：连接池懒加载 + MarketDBError 统一转 503；测试环境无行情库时，连通性任务走"失败"分支并产出可读消息。
5. **图片管道**：上传 → 后缀白名单 → 大小限制 → Pillow 校验/解压 → 长边压缩 → 按年/月/日目录落盘 → 数据库记相对路径；异常时回滚已落盘文件。
6. **SPA 托管**：SPAStaticFiles 捕获 404 回退 index.html，支持前端 history 直达路由刷新；前端使用 hash 模式进一步保证兼容。
7. **部署保留用户数据**：Jenkins rsync 排除 `data/`、`logs/`、`config/app.json`、`.venv/`，避免覆盖运行时数据与本地化配置；`start.sh` 兜底初始化 password.txt。
8. **版本号**：`git rev-list --count HEAD`，部署机 rsync 保留 `.git`（排除了 .git 时回退 "0"）。

## 6. 测试设计

- 单元：password.txt 解析、密码比对、配置脱敏、北京时间归一化、kline 参数校验。
- 接口：登录/登出/me、权限（401/403/降级保护）、日志全生命周期（含坏图、超大图、大图压缩断言）、数据集 CRUD/导入导出（含 GBK、坏行）、任务运行（成功与失败分支）、系统信息/配置。
- 冒烟：健康检查、未登录全接口 401 拦截。
- 隔离：conftest 以 `IDSS_HOME` 指向 pytest 临时目录并重建 app.json/password.txt，生产数据零污染。
- 结果：42 passed。

## 7. 部署设计

- **脚本**：start.sh/ps1（venv 自举 → 依赖安装 → data 初始化 → 后台启动 → 健康检查）、stop、status 同理；PID 记录于 `logs/server.pid`。
- **Jenkins**：`pollSCM('H/30 * * * *')` 轮询 → 停服（脚本+systemd 双通道）→ rsync 更新 `/opt/InvestDecisionSupportSystem`（排除运行时数据）→ pip 安装 → pytest 测试门禁 → 启动 → status 验证。
- **systemd**：`invest-dss.service`（Type=simple，Restart=on-failure，日志 append 到 logs 目录），`systemctl enable --now` 即可托管。
